// Temporary compatibility entry point while the legacy HomeScreen.tsx remains in-tree.
// Phase 11 cleanup can remove the superseded TSX file once the reboot is fully landed.
export { HomeScreenV2 as HomeScreen } from "@/screens/home/HomeScreenV2";
